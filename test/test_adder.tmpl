out() ->
    receive
        {count, N, Version} -> 
            io:format("~p @ ~p ~n", [N, Version])
    end,
    out().

in() ->
    input ! 1,
    timer:sleep(1000),
    io:format("~n", []),
    in().
