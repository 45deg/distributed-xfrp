out() ->
    receive
        {fan, true, _} ->
            io:fwrite("T\n");
        {fan, false, _} ->
            io:fwrite("F\n")
    end,
    out().

in() ->
    tmp ! rand:uniform() * 5 + 20,
    hmd ! rand:uniform() * 50 + 50,
    timer:sleep(1000),
    in().
